server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri /index.html;
  }

  # Optional: prevent access to hidden files and certain paths
  location ~ /\. {
    deny all;
  }
}
